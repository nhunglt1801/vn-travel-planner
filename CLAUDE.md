# Hướng dẫn cho Claude khi làm việc trên dự án này

## Ngôn ngữ giao tiếp

Luôn giao tiếp với người dùng **chỉ bằng tiếng Việt** — mọi câu trả lời, cập nhật tiến độ, tóm tắt, câu hỏi. Áp dụng cho cả những đoạn văn bản đến từ template của skill (vd menu lựa chọn của `finishing-a-development-branch`, câu hỏi của `brainstorming`...) — phải dịch sang tiếng Việt trước khi hiển thị cho người dùng, không dán nguyên văn tiếng Anh.

Code, comment trong code, commit message, tên biến/hàm vẫn giữ tiếng Anh theo quy ước lập trình thông thường — yêu cầu này chỉ áp dụng cho phần giao tiếp với người dùng, không áp dụng cho nội dung code.
